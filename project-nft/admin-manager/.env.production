# 生产环境后端 API 地址 —— 根据部署环境二选一：
# 1. 前端和后端同域 + nginx 反代：留空（默认 /admin/api/v1，靠 nginx location 转发）
# 2. 前后端不同域直连：填后端绝对地址（带 /admin/api/v1 前缀）
#
# 例: VITE_API_BASE_URL=https://api.shuhe-wenchuang.com/admin/api/v1
VITE_API_BASE_URL=/admin/api/v1
